pywebview>=5.3
edge-tts>=7.2
mutagen>=1.47
cmudict>=1.1,<2
pronouncing>=0.2,<1
yt-dlp[default,deno]>=2026.7.4
# Optional JS-page fallback for bounded web research. Vector Radio tries the
# locally installed Edge/Chrome first, so a separate browser download is optional.
playwright>=1.52,<2
tzdata>=2025.2
# Local Ukrainian StyleTTS2. CPU is supported; CUDA is used automatically when available.
git+https://github.com/patriotyk/styletts2-inference@105aed29fa1a7698d08d920986890e9bbd03447c
git+https://github.com/patriotyk/ukrainian-word-stress.git
git+https://github.com/patriotyk/ipa-uk.git
numpy<2
# Optional audio conversion helper
pydub>=0.25
